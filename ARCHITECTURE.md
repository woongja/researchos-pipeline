# Architecture & Design Decisions

단순 스크래퍼가 아니라 **매일 무인 실행되는 파이프라인**으로 설계하면서 내린 판단들을 정리합니다. 핵심은 *"자동화는 실패해도 멈추지 않아야 하고, 자동 생성물은 검증된 데이터와 섞이면 안 된다"* 입니다.

## 1. 데이터 흐름

```
arXiv Atom API ─┐
                ├─▶ 수집·스코어링 ─▶ 태그된 노트(Obsidian) ─┬─▶ 벡터 인덱싱(Chroma) ─▶ RAG 검색
OpenAlex API ───┘   (daily_digest)                        │
                                                          ├─▶ 전문 다운로드 ─▶ 로컬 LLM 요약
                                                          │      (ingest)         (Ollama)
                                                          └─▶ 공개 repo export ─▶ git push
                                                                 (leak-guard, fail-closed)
```

각 단계는 **다음 단계의 실패로부터 독립**입니다. 수집이 성공하면, 요약(Ollama)이 꺼져 있거나 export가 실패해도 그날 수집 결과는 남습니다.

## 2. 단일 소스 취약성 → 다중 소스 fail-soft

**문제**: 초기 버전은 arXiv 단일 API에 의존했습니다. arXiv가 HTTP 429(rate-limit)를 한 번 반환하자 스크립트 전체가 크래시했고, 며칠간 자동 갱신이 밀렸습니다.

**해결**:
- 재시도 계층: `HTTPError`뿐 아니라 `URLError`/`TimeoutError`까지 잡아 백오프 재시도. (`TimeoutError`는 `URLError`의 하위가 아니라는 함정 때문에 초기엔 안 잡혔음.)
- 카테고리 수집 실패 시 **구문 검색만으로 degrade**(전면 중단 X).
- arXiv가 완전히 빈손이면 → **OpenAlex 무키 폴백**(`_fetch_openalex`). arXiv 인프라가 죽어도 다른 소스로 수집 지속. 폴백은 arXiv로 id 해소되는 레코드만 채택해 하위 스키마(불변식)를 보존.

→ 단일 장애점 제거. 정상일 때는 폴백이 발동하지 않아 부하 0.

## 3. 신뢰 3티어 — 자동 생성물 격리

모든 데이터를 동등하게 취급하지 않습니다.

| 티어 | 예시 | 취급 |
|---|---|---|
| 🟢 검증됨 | 사람이 작성한 노트, arXiv 메타데이터 | 그대로 신뢰 |
| 🟡 자동 수집 | API 메타, 스코어링 태그 | 스팟체크 |
| 🔴 LLM 생성 | 로컬 LLM 요약 | **격리 + 배너 필수, 검증 대상** |

- LLM 요약 캐시는 `_sum-*.md`처럼 `_` 접두로 저장해 **벡터 인덱스·목록에서 자동 제외**(RAG 컨텍스트·지식 그래프에 검증 안 된 생성물이 유입되지 않도록).
- 공개 repo에 요약을 노출할 때도 페이지·README에 *"local LLM, unverified"* 배너를 강제.
- 노트 본문에 요약을 인라인하지 않고 **임베드 링크**만 두어 인덱서가 생성물을 원본으로 오인하지 않게 함.

## 4. 비용 0 로컬 LLM 티어

요약·태그 추출 같은 **싼 정형 작업은 로컬 LLM(Ollama·Gemma)** 으로 처리합니다. 클라우드 LLM 폴백을 두지 않아 비용이 0이고 오프라인에서도 동작합니다. 모델은 유휴 시 언로드(`keep_alive` 짧게)하여 GPU VRAM을 다른 작업(모델 학습)에 양보합니다. 깊은 판단이 필요한 작업만 상위 모델로 라우팅하는 **cheap-first** 원칙.

## 5. 프롬프트 인젝션 방어

논문 본문을 LLM 프롬프트에 넣을 때 `_untrusted()` 래퍼로 **"지시가 아닌 데이터"** 임을 명시하고 경계 마커 위조를 제거합니다. 외부 콘텐츠가 요약 프롬프트를 탈취하지 못하도록.

## 6. 유출 방지 (fail-closed)

공개 repo로 내보내기 전, `sync_repos.py`의 **leak-guard**가 생성 파일에서 vault 내부 마커(내부 노트 제목 규칙, 위키링크)를 스캔합니다. 하나라도 걸리면 **push를 중단**(fail-closed). 실제로 한 번 내부 표기가 공개 repo에 새어 나간 사고 이후 도입했습니다. export 자체도 노트 frontmatter만 읽고 본문은 절대 내보내지 않습니다.

## 7. 무의존 · 멱등 · 무인 실행

- **수집·export는 Python 표준 라이브러리만** 사용 — 의존성 설치 없이 어떤 머신에서든 cron으로 실행. (요약·RAG만 무거운 라이브러리 필요, 별도 프로세스로 분리.)
- **멱등성**: 재실행해도 중복 노트·중복 요약·중복 페이지가 생기지 않도록 상태 파일(seen_ids)·frontmatter 마커·존재 체크로 방어. 끊고 다시 돌려도 안전.
- 벡터 인덱싱은 **증분**(mtime 변경 파일만 재임베딩) — 매일 전체 재색인 없이 저비용 유지.

## 8. 티어 분리 배포

- **수집 → 요약 → export**는 각각 독립 스크립트로, `daily_digest`가 subprocess로 체인. 한 단계가 죽어도 경계에서 격리되어 나머지가 진행.
- 무거운 임베딩 모델(BGE-M3)은 상주 서버로 1회 로드, 가벼운 수집 스크립트와 프로세스를 분리해 cron 실행 비용을 낮춤.
